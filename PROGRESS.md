# PROGRESS.md — append-only log, one line per completed task

- A1: Created and checked out branch `web-app-build` from `main` (tip be109d9, includes 7fe0015 logic-layer port). Working tree was clean. Files: none (branch op only).
- A2: `git fetch origin` succeeded. origin/main tip is 7fe0015, already contained in local main (be109d9 is a local-only chore commit not yet pushed). No new upstream commits, nothing merged. Files: none.
- A3: Ran `npx tsc --noEmit && npx next build` on the untouched tree. Both exit 0 (Next.js 16.2.11 Turbopack build, static pages / and /_not-found generated). Baseline confirmed green. Files: none.
- A4: Ported colors.ts/spacing.ts/typography.ts/radius.ts from Scantrix_v2 into a Tailwind v4 `@theme` block. Dropped scaffold's dark-mode override (mobile source is light-only). Gate green. Files: src/app/globals.css.
- A5: Built Button ('use client', variants primary/secondary/outline/danger, loading spinner), Card/Input/Badge (server components) in src/components/ui/, all styled from A4's tokens (bg-primary, text-trust-navy, border-border, rounded-md/lg/pill, var(--space-*) paddings). Styling conventions (card padding, input height 50px, primary teal buttons) cross-checked against Scantrix_v2 LoginScreen.tsx for visual parity. Gate green. Files: src/components/ui/{Button,Card,Input,Badge,index}.tsx.
- A6: Grepped src/lib/ + src/store/ for outbound email/SMS/payment-send capability. None found — recorded in ASSUMPTIONS.md. Files: ASSUMPTIONS.md only.
- A7: Created gitignored .env.local with real NEXT_PUBLIC_API_URL, NEXT_PUBLIC_QUICKBOOKS_API_URL, NEXT_PUBLIC_FIREBASE_* (documentation parity), and a real NEXT_PUBLIC_GOOGLE_CLIENT_ID found hardcoded as `webClientId` in Scantrix_v2's LoginScreen.tsx/CreateAccountScreen.tsx — this reverses the pre-marked BLOCKED status for web Google Sign-In (see ASSUMPTIONS.md for the full reasoning and the Cloud Console origin-allowlist caveat). Apple Sign-In pre-mark stands. Files: .env.local (gitignored, not committed).
- B1: Enumerated all 19 screens under Scantrix_v2 src/screens/**/*.tsx into STATUS.md with one row each (mobile path, summary, status). Found 7 MISSING screens not explicitly named by existing Phase C tasks (VerifyOTP, InvoiceList, InvoiceDetails, InvoicePreview, PendingInvoicesScreen, EditProfile, ProfileOptionsScreen hub) and appended C13-C19 to TASKS.md Phase C for them. SplashScreen needs no new task — subsumed by C2's auth-gate loading state. Files: STATUS.md (new), TASKS.md (Phase C appended, per LOOP.md's self-audit rule).
- C1: Built /login and /register pages. LoginForm/RegisterForm are 'use client' components wrapped in Suspense (useSearchParams prerendering requirement) inside server page.tsx shells. Real Google Identity Services wiring via GoogleSignInButton (renders the official GIS button, posts idToken to googleLogin thunk) since A7 found a real web client ID; Apple Sign-In renders a disabled "Coming Soon" button. Discovered VerifyOTPScreen.tsx never actually uses the `password` route param CreateAccountScreen passes it — so register->verify-otp only carries `email`+`inviteToken` via query params, avoiding ever putting a password in a URL. React Native Alert.alert -> window.alert/confirm (see new cross-cutting ASSUMPTIONS entry). Full country-code list ported to src/lib/countryCodes.ts. Gate green (tsc + next build, /login and /register both statically prerendered). Files: src/store/hooks.ts, src/lib/countryCodes.ts, src/components/auth/{LoginForm,RegisterForm,GoogleSignInButton,AppleSignInButton}.tsx, src/app/login/page.tsx, src/app/register/page.tsx.
- C2: Built AuthGate ('use client'), wired into root layout inside Providers (layout.tsx itself stays a Server Component). On mount it replays App.tsx's restoreSession effect (getUser() -> dispatch restoreUser()), showing a full-screen loader meanwhile — this is what subsumes SplashScreen's job on web (STATUS.md row 1). After restore, redirects unauthenticated users on any non-public route to /login, and authenticated users away from /login or /register to /dashboard. /invite/accept and /register/verify-otp are public routes since they have their own internal auth-aware logic (C8, C13). Verified via next build (static prerender, no SSR crash) and a dev-server smoke request (200s, no server errors); curl showed only RSC flight-stream markup since the whole tree is client components, which is expected and not itself verifiable without a real browser — full UI verification deferred to Phase D's dev-server pass. Gate green. Files: src/components/auth/AuthGate.tsx, src/app/layout.tsx.
- C3: Built /dashboard (DashboardContent client component + thin server page.tsx). Fixed a real cross-platform bug in scanInvoice (invoiceApi.ts) — its FormData used an RN-only {uri,name,type} shape that would upload garbage in a real browser; now takes a browser File directly. Wired getInvoices/getMyQBConnections on mount, a 3s poll while any invoice is "processing", the QuickBooks-not-connected banner (connectToQuickBooks() redirect), pending-review card, auto/manual/failed summary cards (linking to /invoices?type=X, C14), recent invoices list (non-interactive, matching mobile's dead onPress), and a file-input upload trigger (image/pdf) replacing mobile's camera/gallery/PDF action sheet. New shared src/lib/invoiceDisplay.ts consolidates per-status theming that mobile had drifted across 3 separate screens. QB company switcher deliberately deferred to C12's shell rather than duplicated here (see ASSUMPTIONS.md). Gate green (tsc + next build; /dashboard statically prerendered). Files: src/store/invoice/invoiceApi.ts (fix), src/lib/invoiceDisplay.ts, src/components/dashboard/DashboardContent.tsx, src/app/dashboard/page.tsx.
- C4: Built /invoices/[id]/review (InvoiceReviewContent client component + async server page.tsx awaiting Next 16's Promise-based params). Confidence-themed (auto/review/manual bands, hex-for-hex ported into src/lib/invoiceReviewTheme.ts), vendor/currency/GL-account selects, editable fields with validation matching mobile, reject/post-to-QuickBooks actions via window.confirm. Normalized against the real ExtractedData/PostInvoiceExtractedData contract rather than mobile's drifted RawInvoiceData type (dropped a dead "Tax Type" field, fixed "Item Descriptions" to map to the real `description` field). Also found and dropped genuinely dead code from mobile's vendorId fallback chain (referenced nonexistent CreatedVendor fields). Post/reject success now returns to /invoices/pending instead of mobile's "MainTabs" catch-all. Gate green (tsc + next build; /invoices/[id]/review correctly built as a dynamic server-rendered route). Files: src/lib/invoiceReviewTheme.ts, src/lib/currencies.ts, src/components/invoices/InvoiceReviewContent.tsx, src/app/invoices/[id]/review/page.tsx.
- C5: Built /invoices/[id]/vendor (VendorResolutionContent client component + async server page.tsx). Suggested/All/Create tabs with radio-select rows and sticky "Use this vendor" confirm footer, create-vendor form (name/currency/optional GL account/optional tax code) dispatching createQuickBooksVendor then setSelectedVendor. Fixed a type mismatch: mobile's screen-local Vendor type assumed address/email/phone fields that don't exist on the real ported Vendor type (quickBooksSlice.ts) — vendor rows show display name only, matching the real contract. Also dispatches getInvoiceDetails(invoiceId) on mount for direct-URL-load robustness. Gate green (tsc + next build; /invoices/[id]/vendor built as a dynamic route). Files: src/components/invoices/VendorResolutionContent.tsx, src/app/invoices/[id]/vendor/page.tsx.
- C6: Built /quickbooks (QuickBooksConnectContent). Connections list with active/switch/disconnect, "Connect QuickBooks"/"Add Another Account" via the already-ported connectToQuickBooks() plain redirect (no polling/openAuthSession dance needed — that mobile complexity was working around in-app-browser quirks that don't exist on a full-page web redirect). Re-checks status on window focus as the web equivalent of mobile's AppState listener. Split out as a standalone page from AccountingSoftwaresScreen's combined UI so C9 can link here for "manage connections" instead of duplicating a bottom-sheet modal (see ASSUMPTIONS.md). Gate green (tsc + next build; /quickbooks statically prerendered). Files: src/components/quickbooks/QuickBooksConnectContent.tsx, src/app/quickbooks/page.tsx.
- C7: Built /team (TeamMembersContent). Active-company card, invite card (owner/admin gated) vs. permission notice, member list with role badges and role-gated remove, invite modal (email + role radio picker) dispatching inviteQBMember/fetchQBMembers/removeQBMember. Empty state links to /quickbooks when no company is connected. Gate green (tsc + next build; /team statically prerendered). Files: src/components/team/TeamMembersContent.tsx, src/app/team/page.tsx.
- C8: Built /invite/accept (InviteAcceptContent client, Suspense-wrapped for useSearchParams). No-token -> /login; unauthenticated -> savePendingInviteToken then /login?fromInvite=true; authenticated -> acceptQBInvite with checking/success/error states. Gate green (tsc + next build; /invite/accept statically prerendered). Files: src/components/auth/InviteAcceptContent.tsx, src/app/invite/accept/page.tsx.
- C9: Built /accounting-software (AccountingSoftwaresContent). QuickBooks card shows live connected/checking/not-connected status and links to /quickbooks; Google Drive card is a client-only localStorage mock (no network call — see ASSUMPTIONS.md for the discrepancy found and deliberately not acted on: mobile source actually calls real /google-drive/connect+status endpoints, but the pre-seeded scope explicitly said not to build that tonight); Tally/Zoho Books disabled "Coming Soon" cards. Gate green (tsc + next build; /accounting-software statically prerendered). Files: src/components/accounting/AccountingSoftwaresContent.tsx, src/app/accounting-software/page.tsx.
- C10: Built /profile (ProfileContent) with just the Delete Account entry point for now — a real window.confirm dialog matching mobile's text verbatim, but the action itself stays a disabled "Coming Soon" stub (no backend endpoint exists). C19 will extend this same page with the rest of the ProfileOptionsScreen hub (profile card, settings/legal/support sections, logout) rather than building a separate page that gets replaced. Gate green (tsc + next build; /profile statically prerendered). Files: src/components/profile/ProfileContent.tsx, src/app/profile/page.tsx.
- C11: Built /plans, /subscription, /subscription/paywall. Exact mock pricing/numbers ported verbatim, monthly/yearly toggle, "Preview only" alerts on every plan/upgrade action, no real billing calls anywhere. SubscriptionStatusContent stays a Server Component (no hooks/handlers of its own). Gate green (tsc + next build; all three statically prerendered). Files: src/components/subscription/{PlansContent,SubscriptionStatusContent,SubscriptionPaywallContent}.tsx, src/app/plans/page.tsx, src/app/subscription/page.tsx, src/app/subscription/paywall/page.tsx.
- C12: Built AppShell (left sidebar: Dashboard/Invoices/QuickBooks/Team/Accounting Software/Subscription nav, QB company switcher moved here from C3, Account/Logout footer via new shared useLogout() hook). Wired into AuthGate as a runtime decision (isAuthenticated + NO_SHELL_ROUTES pathname check) rather than a Next.js route group, to avoid a real physical-folder conflict risk between a grouped "invoices" tree (C14/C15, not yet built) and the already-existing ungrouped /invoices/[id]/review and /invoices/[id]/vendor routes. Consequence: those two now get the shell too (simplification vs. mobile's full-screen push). Renamed /subscription/paywall -> /paywall (top-level) to keep it shell-free without any folder-nesting ambiguity. "/" now redirects to /login or /dashboard; the stale create-next-app placeholder is gone. Gate green (tsc + next build after clearing a stale .next/types reference from the paywall rename; dev-server smoke test on /, /dashboard, /paywall, /quickbooks all 200 with no server errors). Files: src/components/shell/AppShell.tsx, src/store/useLogout.ts, src/components/auth/AuthGate.tsx, src/app/page.tsx, src/app/paywall/page.tsx (moved), src/components/subscription/PlansContent.tsx (link update).
- C13: Built /register/verify-otp (VerifyOtpContent, Suspense-wrapped). 6 auto-advancing OTP boxes, 60s resend countdown, verifyRegisterOtp -> acceptQBInvite (if inviteToken present) -> /dashboard. Gate green (tsc + next build; /register/verify-otp statically prerendered). Files: src/components/auth/VerifyOtpContent.tsx, src/app/register/verify-otp/page.tsx.
- C14: Built /invoices?type=auto|manual|failed (InvoiceListContent, Suspense-wrapped). Per-status themed header/list (extended src/lib/invoiceDisplay.ts with accentHex/accentTextClass rather than a separate THEME_CONFIG copy), summary strip, empty state, row click sets selectedInvoice and navigates to /invoices/[id] (C15, forward reference). dispatches getInvoices() on mount for direct-load robustness. Gate green (tsc + next build; /invoices statically prerendered). Files: src/lib/invoiceDisplay.ts (extended), src/components/invoices/InvoiceListContent.tsx, src/app/invoices/page.tsx.
- C15: Built /invoices/[id]?type=... (InvoiceDetailContent, Suspense-wrapped async server page.tsx). Hero card (status/confidence/amount/progress bar), Invoice Information/Financial Summary/Vendor Details/Item Descriptions/Line Items/Status History sections, "View Original Invoice" link forward-referencing /invoices/preview (C16). New src/lib/invoiceDetailTheme.ts (hex-for-hex ported per-status theme, distinct from invoiceDisplay.ts's simpler shared theme). Confirms C12's route-nesting call was sound: [id]/page.tsx sits alongside [id]/review and [id]/vendor with zero conflict. Gate green (tsc + next build; /invoices/[id] built as a dynamic route). Files: src/lib/invoiceDetailTheme.ts, src/components/invoices/InvoiceDetailContent.tsx, src/app/invoices/[id]/page.tsx.
- C16: Built /invoices/preview?url=...&mimeType=... (InvoicePreviewContent, Suspense-wrapped) — a direct port of Scantrix_v2's own InvoicePreviewScreen.web.tsx (iframe for PDF, img for image), used from both C4 and C15. Gate green (tsc + next build; static route coexists cleanly with the [id] dynamic segment). Files: src/components/invoices/InvoicePreviewContent.tsx, src/app/invoices/preview/page.tsx.
- C17: Built /invoices/pending (PendingInvoicesContent). Vendor/number/amount summary, single most-important-issue pill, manual refresh button, row click clears vendor selection state then routes to /invoices/[id]/review. Gate green (tsc + next build; /invoices/pending statically prerendered). Files: src/components/invoices/PendingInvoicesContent.tsx, src/app/invoices/pending/page.tsx.
- C18: Built /profile/edit (EditProfileContent). Real Firebase Auth updateProfile + Firestore setDoc(merge) calls, photo upload via a real file input dispatching updateProfileIcon (not the throwing pickProfileImage() stub, which explicitly documents itself as superseded by exactly this UI). Fixed a second instance of the RN-FormData-shape bug class found in C3, this time in authApi.ts's updateProfileIcon (uri/name/type object + boundary-less manual Content-Type header -> real File + browser-set Content-Type). Gate green (tsc + next build; /profile/edit statically prerendered). Files: src/store/auth/authApi.ts (fix), src/components/profile/EditProfileContent.tsx, src/app/profile/edit/page.tsx.
